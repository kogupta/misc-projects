<?xml version='1.0'?>

<coherence xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns="http://xmlns.oracle.com/coherence/coherence-operational-config"
           xsi:schemaLocation="http://xmlns.oracle.com/coherence/coherence-operational-config
   coherence-operational-config.xsd">
  <cluster-config>
    <member-identity>
      <cluster-name system-property="tangosol.coherence.cluster">chump-cluster
      </cluster-name>
      <member-name system-property="tangosol.coherence.member">chumpWorker
      </member-name>
    </member-identity>

    <!-- single server mode -->
    <unicast-listener>
      <address system-property="tangosol.coherence.localhost">127.0.0.1
      </address>
    </unicast-listener>
    <multicast-listener>
      <time-to-live system-property="tangosol.coherence.ttl">0</time-to-live>
    </multicast-listener>
    <!-- single server mode ends -->

    <!--<multicast-listener>-->
      <!--<address>224.0.0.1</address>-->
      <!--<time-to-live>0</time-to-live>-->
    <!--</multicast-listener>-->

  </cluster-config>

  <configurable-cache-factory-config>
    <init-params>
      <init-param>
        <param-type>java.lang.String</param-type>
        <param-value system-property="coherence.cacheconfig">
          example-config.xml
        </param-value>
      </init-param>
    </init-params>
  </configurable-cache-factory-config>
</coherence>
